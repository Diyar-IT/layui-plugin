(function () {
  var plaintext = (function (hljs) {
      return {
        name: 'Plain text',
        aliases: [
          'text',
          'txt'
        ],
        disableAutodetect: true
      };
    }
  );
  hljs.registerLanguage("plaintext", plaintext);
})();